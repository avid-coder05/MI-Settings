<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:id="@+id/contentPanel"
    style="@style/dialog_btn_vertical_group">
    <Button
        android:textSize="@dimen/text_font_size_51"
        android:id="@+id/wifi_candidate"
        android:visibility="gone"
        style="@style/dialog_vertical_btn"/>
    <Button
        android:textSize="@dimen/text_font_size_51"
        android:id="@+id/wifi_negative"
        style="@style/dialog_vertical_btn"/>
    <Button
        android:textSize="@dimen/text_font_size_51"
        android:id="@+id/wifi_positive"
        style="@style/dialog_suggest_vertical_btn"/>
</LinearLayout>
